# Multi-tenant Cluster

- [Multi-tenant Cluster](#multi-tenant-cluster)
  - [Goal](#goal)
  - [Layers](#layers)
  - [Capabilities](#capabilities)
  - [Boundary — platform vs. tenant](#boundary--platform-vs-tenant)
  - [Onboarding](#onboarding)
  - [Demo - Team A](#demo---team-a)
  - [Demo - Team B](#demo---team-b)

## Goal

Create a **multi-tenant cluster** with AWS EKS.
List fundamental capabilities provided by platform

- compute
- storage
- gateway
- dns

Define a clear boundary between the **platform** and its **tenants**.
Demo team onboarding:

- team a: simple stateless web app
- team b: full-stack to-do app

---

## Layers

| Layer        | Role                                                                                | Tooling                                                            |
| ------------ | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| **Platform** | AWS foundation and cluster capabilities (compute, network, security, observability) | Terraform, Karpenter, Istio, ALBC, ESO, external-dns, cert-manager |
| **Tenant**   | Individual application workloads                                                    | Helm / manifests                                                   |

---

## Capabilities

- **Compute — Karpenter**

| Node class | Instance family | Purpose            |
| ---------- | --------------- | ------------------ |
| general    | m6i / m7i       | Default workloads  |
| database   | r6i / i4i       | High-I/O, stateful |
| gpu        | g5 / g6         | ML / AI workloads  |

- **Storage — EBS CSI**

| Class      | Type | Reclaim | Use                      |
| ---------- | ---- | ------- | ------------------------ |
| `gp3`      | gp3  | Delete  | Default block storage    |
| `gp3-iops` | gp3  | Retain  | Databases, WAL, stateful |

- **Network**

| Concern             | Tooling                      |
| ------------------- | ---------------------------- |
| CNI + NetworkPolicy | VPC CNI (NP enabled)         |
| Gateway API         | Istio ambient                |
| Load Balancer       | AWS Load Balancer Controller |
| DNS                 | external-dns                 |

- **Security**

| Concern          | Tooling       |
| ---------------- | ------------- |
| Sensitive data   | ESO           |
| mTLS             | Istio ambient |
| Network policy   | VPC CNI       |
| TLS certificates | cert-manager  |
| Admission policy | Kyverno       |

- **Observability**

defer

- **GitOps**

defer

---

## Boundary — platform vs. tenant

The platform owns the capability; the tenant owns the workload that consumes it.

| Capability | Platform owns                                                                 | Tenant owns                                                                 |
| ---------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| Compute    | Karpenter `NodePool` + `EC2NodeClass`, node labels/taints, autoscaling        | Pod spec, `resources`, `nodeSelector`, `tolerations`, HPA                    |
| Storage    | `StorageClass` (`gp3`, `gp3-iops`), EBS CSI driver                            | `PVC` (size, `storageClassName`), volume expansion                          |
| Network    | Shared `Gateway` on NLB, wildcard TLS (`*.arguswatcher.net`), Route 53 zone  | `HTTPRoute` (hostname within team subdomain, path/backend rules), `Service` |

Tenants never touch: NodePools, StorageClasses, the Gateway, Route 53 records, TLS certificates, or the load balancer.

The tenant consumption pattern is uniform — pick a class or attach to a shared resource by reference:

```yaml
# Compute — opt into database class
nodeSelector: { workload-class: database }
tolerations:
  - { key: workload-class, value: database, effect: NoSchedule }

# Storage — pick a StorageClass
storageClassName: gp3-iops

# Network — attach to the shared Gateway
parentRefs: [{ name: istio-ingress, namespace: istio-ingress }]
hostnames: [ team-a.arguswatcher.net ]
```

---

## Onboarding

**What the tenant brings**

1. Team name (`<team>`) — used as namespace, subdomain, and label.
2. Git repo containing manifests.

**What the platform provisions** — one commit under `argocd/tenants/<team>/`:

1. Namespace `<team>` with label `team=<team>`.
2. ArgoCD `AppProject` scoped to the tenant's repo and namespace.
3. ArgoCD `Application` pointing at the tenant's manifests path.

**What the tenant deploys** — one PR against their own repo:

- `Deployment` / `StatefulSet` with `team` label, `resources`, and probes.
- `PVC` (if stateful) referencing `gp3` or `gp3-iops`.
- `Service` fronting the pods.
- `HTTPRoute` attached to `istio-ingress/istio-ingress`, hostname under `<team>.arguswatcher.net`.

**Automatic on admission** — no tenant or platform action:

- Karpenter provisions matching nodes.
- CSI provisions the EBS volume in the pod's AZ.
- external-dns creates the Route 53 record.
- Wildcard TLS already covers the hostname.

---

## Demo - Team A

**Profile.** Simple stateless web app. Default path only — no PVC, no toleration, one hostname.

**Capabilities exercised:** compute (`general`), ingress + TLS + DNS.

- Application
  - simple nginx web app
  - stateless
  - default nginx
  - file: `demo-app/team-a`
  - host: `team-a.arguswatcher.net`

**Tenant manifests**

```yaml
# demo-app/team-a/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: team-a
  labels: { team: team-a, app: web }
spec:
  replicas: 2
  selector: { matchLabels: { app: web } }
  template:
    metadata: { labels: { team: team-a, app: web } }
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports: [{ containerPort: 80, name: http }]
          resources: { requests: { cpu: 50m, memory: 64Mi } }
          readinessProbe: { httpGet: { path: /, port: http } }
          livenessProbe:  { httpGet: { path: /, port: http } }
---
apiVersion: v1
kind: Service
metadata: { name: web, namespace: team-a }
spec:
  selector: { app: web }
  ports: [{ port: 80, targetPort: http, name: http }]
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: web, namespace: team-a }
spec:
  parentRefs: [{ name: istio-ingress, namespace: istio-ingress }]
  hostnames: [ team-a.arguswatcher.net ]
  rules:
    - backendRefs: [{ name: web, port: 80 }]
```

**Steps**

1. Platform creates `AppProject/team-a` and `Application/team-a` (points at `demo-app/team-a`).
2. Tenant pushes manifests; ArgoCD syncs.
3. Karpenter provisions a `general` node if none is available.
4. external-dns creates `team-a.arguswatcher.net` → NLB.
5. Verify: `curl https://team-a.arguswatcher.net` returns the nginx welcome page.

---

## Demo - Team B

**Profile.** Full-stack to-do app: web tier (stateless) + Postgres (stateful, PVC). Exercises two node classes and high-IOPS block storage.

**Capabilities exercised:** compute (`general` + `database`), storage (`gp3-iops`, `Retain`), ingress + TLS + DNS.

- Application
  - full-stack to-do app
  - stateful, PVC
  - file: `demo-app/team-b` (renamed from `demo-app/to-do-app`)
  - host: `team-b.arguswatcher.net`

**Tenant manifests**

```yaml
# Postgres — database class, gp3-iops PVC
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: team-b
  labels: { team: team-b, app: postgres }
spec:
  serviceName: postgres
  replicas: 1
  selector: { matchLabels: { app: postgres } }
  template:
    metadata: { labels: { team: team-b, app: postgres } }
    spec:
      nodeSelector: { workload-class: database }
      tolerations:
        - { key: workload-class, value: database, effect: NoSchedule }
      containers:
        - name: postgres
          image: postgres:16
          env:
            - { name: POSTGRES_PASSWORD, value: changeme }  # ESO in a later phase
          ports: [{ containerPort: 5432, name: pg }]
          volumeMounts: [{ name: data, mountPath: /var/lib/postgresql/data }]
          resources: { requests: { cpu: 250m, memory: 512Mi } }
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ReadWriteOnce]
        storageClassName: gp3-iops
        resources: { requests: { storage: 20Gi } }
---
apiVersion: v1
kind: Service
metadata: { name: postgres, namespace: team-b }
spec:
  clusterIP: None
  selector: { app: postgres }
  ports: [{ port: 5432, targetPort: pg, name: pg }]
---
# Web tier — general class, no toleration
apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo
  namespace: team-b
  labels: { team: team-b, app: todo }
spec:
  replicas: 2
  selector: { matchLabels: { app: todo } }
  template:
    metadata: { labels: { team: team-b, app: todo } }
    spec:
      containers:
        - name: todo
          image: ghcr.io/team-b/todo:1.0.0
          ports: [{ containerPort: 8080, name: http }]
          env:
            - { name: DB_HOST, value: postgres.team-b.svc.cluster.local }
          resources: { requests: { cpu: 100m, memory: 128Mi } }
          readinessProbe: { httpGet: { path: /healthz, port: http } }
          livenessProbe:  { httpGet: { path: /healthz, port: http } }
---
apiVersion: v1
kind: Service
metadata: { name: todo, namespace: team-b }
spec:
  selector: { app: todo }
  ports: [{ port: 80, targetPort: http, name: http }]
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata: { name: todo, namespace: team-b }
spec:
  parentRefs: [{ name: istio-ingress, namespace: istio-ingress }]
  hostnames: [ team-b.arguswatcher.net ]
  rules:
    - backendRefs: [{ name: todo, port: 80 }]
```

**Steps**

1. Platform creates `AppProject/team-b` and `Application/team-b` (points at `demo-app/team-b`).
2. Tenant pushes manifests; ArgoCD syncs.
3. Karpenter provisions two nodes: one `general` (web tier), one `database` (Postgres).
4. CSI provisions a 20 GiB `gp3-iops` volume in Postgres's AZ.
5. external-dns creates `team-b.arguswatcher.net` → NLB.
6. Verify: `curl https://team-b.arguswatcher.net` returns the to-do UI; Postgres pod stays scheduled only on the `database` node.
